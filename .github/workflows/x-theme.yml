name: x-theme

on:
  push:
    branches: [main]
    paths:
      - "extensions/x-matrix-theme/**"
      - ".github/workflows/x-theme.yml"
  workflow_dispatch:
  release:
    types: [created]

permissions:
  contents: read

jobs:
  zip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Zip the X theme
        run: |
          cd extensions
          zip -r ../x-matrix-theme.zip x-matrix-theme -x "*/.DS_Store" "*/__pycache__/*"
          unzip -l ../x-matrix-theme.zip | head -20
          ls -lh ../x-matrix-theme.zip
      - uses: actions/upload-artifact@v4 # v4.6.2 — pinned via tag for now (SHA 65c4c4a... fails to resolve in this env, will re-pin to SHA after verify)
        with:
          name: x-matrix-theme
          path: x-matrix-theme.zip
          if-no-files-found: error
